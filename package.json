{
  "name": "pxh-dubbing-yootob",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && tsc -p tsconfig.api.json --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "dev": "vite",
    "dev:api": "tsx scripts/local-api.ts",
    "migrate:global-translations": "tsx scripts/migrate-global-translations.ts",
    "check": "tsc --noEmit && tsc -p tsconfig.api.json --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@vercel/node": "^4.0.0",
    "groq-sdk": "^0.37.0",
    "msedge-tts": "^2.0.4",
    "youtube-transcript": "^1.3.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/chrome": "^0.1.32",
    "@types/node": "^24.10.1",
    "tsx": "^4.23.9",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20"
  }
}
